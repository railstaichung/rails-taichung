<div class="container">
  <div class="row">
    <section class="content">
      <h1>問題清單</h1>
      <div class="col-md-10 col-md-offset-1">
        <div class="panel panel-default">
          <div class="panel-body">
            <% if current_user %>
              <div class="pull-left">
                <div class="btn-group">
                  <%= link_to("Add issue", new_issue_path , class: "btn btn-primary") %>
                </div>
              </div>
            <% end %>
            <div class="pull-right">
              <div class="btn-group">
                <button type="button" class="btn btn-success btn-filter" data-target="open">Open issues</button>
								<button type="button" class="btn btn-warning btn-filter" data-target="closed">Closed issues</button>
								<button type="button" class="btn btn-default btn-filter" data-target="all">All issues</button>
              </div>
            </div>
            <div class="table-container">
              <table class="table table-filter">
                <tbody>
                  <% @issues.each do |issue| %>
                    <tr data-status="open">
                      <td>
                        <div class="media">
                          <a href="#" class="pull-left">
                            <%= image_tag issue.owner.gravatar_url(:default => 'https://dl.dropboxusercontent.com/u/1252720/_rortc/ROR.jpg'), class:"media-photo"%>
                          </a>
                          <div class="media-body">
                            <span class="media-meta pull-right"><%= issue.created_at.to_s(:long) %></span>
                            <h4 class="title">
                              <%= link_to("#{issue.title}##{issue.id}", issue_path(issue)) %>
                              <span class="pull-right open">(Open)</span>
                            </h4>
                            <p class="summary">
                              <%= sanitize_text2(issue.content) %>
                            </p>
                          </div>
                        </div>
                      </td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </section>
  </div>
</div>
<script type="text/javascript">
$(document).ready(function() {
  $('.btn-filter').on('click', function () {
    var $target = $(this).data('target');
    if ($target != 'all') {
      $('.table tr').css('display', 'none');
      $('.table tr[data-status="' + $target + '"]').fadeIn('slow');
    } else {
      $('.table tr').css('display', 'none').fadeIn('slow');
    }
  });

});
</script>
