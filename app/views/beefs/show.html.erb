<div class="container-fluid" style="padding: 15px 30px 15px 50px;">
  <div class="row">

    <div class="col-md-12">
      <div class="group">
        <%= link_to("編輯", edit_beef_path(@beef), class: "btn btn-primary pull-right")%>
      </div>
      <h2><%= @beef.title %></h2>
      <p><%= @beef.description %></p>
      <p>標籤：<%= raw @beef.tag_list.map { |t| link_to t, tag_path(t) }.join(', ') %></p>
    </div>

  </div>
  <%= render "common/disqus" %>
</div>
